<div class="container">
  <h1>Edit <%= @cocktail.name %></h1>
  <%= link_to "Cancel", cocktail_path(@cocktail) %>
  <%= simple_form_for(@cocktail) do |f| %>
    <%= f.input :name %>
    <%= f.submit %>
  <% end %>
</div>
